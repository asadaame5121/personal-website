name: Update Dailylog

on:
  push:
    branches: [main]

jobs:
  update-dailylog:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: 'recursive'
          token: ${{ secrets.PAT_FOR_COMMIT }}


      - name: Set up Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Run update-dailylog script
        run: deno run --allow-read --allow-write scripts/update-dailylog.ts

      - name: Commit and push changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add data/dailylog.md
          if git diff --cached --quiet; then
            echo 'No changes to commit.'
          else
            git commit -m 'Update dailylog.md [GitHub Actions]'
            git push
          fi

